# Local Claude Code session state — not part of the project
.claude/

# OS/editor cruft
.DS_Store
Thumbs.db

# Wrangler local state (Cloudflare Worker deploy tooling)
oauth-worker/.wrangler/

# Keystatic editor app (Next.js)
/node_modules
/.next
next-env.d.ts
*.tsbuildinfo

# Secrets — .env.example is tracked, real values never are
.env
.env*.local

# Preview service runtime state
/preview-service/node_modules
/preview-service/.cache

# Cloudflare / OpenNext build output
/.open-next
/.wrangler
