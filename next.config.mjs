/** @type {import('next').NextConfig} */
const nextConfig = {
  // The Keystatic admin is the only thing this app serves. The public site is
  // still rendered by the Docusaurus copy in ./website. Nothing here touches it.
};

export default nextConfig;
